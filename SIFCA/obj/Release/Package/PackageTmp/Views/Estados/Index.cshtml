@model IEnumerable<SIFCA.Models.Estados>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESCPR)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESCPR)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CODEDO }) |
            @Html.ActionLink("Details", "Details", new { id=item.CODEDO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CODEDO })
        </td>
    </tr>
}

</table>
